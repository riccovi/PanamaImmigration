import { View } from 'react-native';
import BottomBar from '../components/BottomBar';

function Profile({}){
    return (
      <View>

        <BottomBar />
      </View>
    )
}

export default Profile;